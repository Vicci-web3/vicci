{
  "name": "@vic/targeting-agent",
  "version": "0.0.1",
  "scripts": {
    "dev": "tsx watch --clear=false src/index.ts",
    "build": "tsc",
    "test": "jest",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0",
    "@vic/shared": "workspace:^",
    "amqplib": "^0.10.5"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.6",
    "@types/node": "^22.1.0",
    "tsx": "^4.7.0",
    "typescript": "~5.6.3",
    "prisma": "^6.3.0"
  }
}